import type { Course } from '../types'

// semantic — course 09 of the nine-course dbt spine. Semantic Layer, cost and the AI surface — 9 sections planned.
// Declared empty so the arc is visible in the app; sections land as this slice is authored.
// The section plot is in ../../../COURSE-PLAN.md.
export const semantic: Course = {
  id: 'semantic',
  title: 'Semantic Layer & Beyond',
  sections: [],
}
